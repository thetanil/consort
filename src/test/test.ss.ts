import { describe } from 'mocha';
import { assert } from 'chai';

describe("SS", () => {
    describe("emit test", () => {
        it("should emit", () => {
            assert.equal(1, 1)
        })
    })
})